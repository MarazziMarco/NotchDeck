# Screenshots

The README references four screenshots that are **not yet in the repository**.
No safe, privacy-scrubbed app screenshots were found during publication, so they
were intentionally **not invented**.

To complete the README gallery, capture the real Debug app and add these files
here (exact names, case-sensitive):

| File          | Screen                          |
| ------------- | ------------------------------- |
| `home.png`    | Utilities → Home                |
| `focus.png`   | Utilities → Focus (Pomodoro)    |
| `files.png`   | Utilities → Files / File Shelf  |
| `agents.png`  | Agents face                     |

Before adding a screenshot:

- remove personal terminal commands and usernames where unnecessary;
- remove local absolute paths and sensitive file names;
- do **not** enable the DEBUG geometry overlays;
- use the real app (no mockups);
- keep text readable while optimizing file size.

Once added, the README's gallery links will render automatically.
